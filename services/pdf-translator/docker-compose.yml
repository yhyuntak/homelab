services:
  pdf-translator:
    build: .
    volumes:
      - ./data/input:/app/data/input
      - ./data/output:/app/data/output
      - ./src:/app/src
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
